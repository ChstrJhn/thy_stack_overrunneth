<% provide(:title, @user.name)%>
<div class="row">
	<aside class="col-md-4">
		<section class="user_info">
<h1>
  <%= gravatar_for @user %>
  <%= @user.name %>
</h1>
        </section>
    </aside>
</div>
<p>Got a question? <%= link_to 'Ask now!', new_question_path %></p>

<%= link_to 'Edit', edit_user_path(@user) %> |
<%= link_to 'Back', root_url %>